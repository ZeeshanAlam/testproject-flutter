import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(
    debugShowCheckedModeBanner: false,
    home: scrVideo(),
  ));
}

class scrVideo extends StatefulWidget {
  const scrVideo({Key? key}) : super(key: key);

  @override
  _scrVideoState createState() => _scrVideoState();
}

class _scrVideoState extends State<scrVideo> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
